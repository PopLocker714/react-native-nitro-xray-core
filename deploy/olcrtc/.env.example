# Copy to .env and fill in. `docker compose` reads this automatically.

# Prebuilt image to pull (defaults to jojo714/olcrtc:latest in the compose).
OLCRTC_IMAGE=jojo714/olcrtc:latest

# Carrier: wbstream | telemost | jitsi. wbstream is RF-whitelisted and works.
# jitsi is NOT usable — meet.jit.si now requires a JWT.
OLCRTC_CARRIER=wbstream

# wbstream account token (Bearer eyJ...). WITHOUT it the relay runs as an
# anonymous guest, which LiveKit forbids from (re)creating rooms — "guests
# cannot create rooms" — so it dies in a 403 loop the moment the room is GC'd.
# Set a WB account access token so the session holds create/publish rights and
# keeps the room alive. Grab it: log into stream.wb.ru, DevTools -> Network ->
# any api-room request -> copy the "Authorization: Bearer eyJ..." header value.
# The token is long-lived (no exp; dies only when you log that account out) and
# not IP-bound. The x_wbaas_token cookie is NOT this — it 401s as a Bearer.
OLCRTC_AUTH_TOKEN=

# Transport — MUST match the mobile client. wbstream/telemost need vp8channel
# (datachannel doesn't work on them). vp8channel is the client's native default.
OLCRTC_TRANSPORT=vp8channel

# For wbstream, room = the BARE room UUID from https://stream.wb.ru — NOT the
# full URL. Use "019f32bf-...", not "https://stream.wb.ru/room/019f32bf-...".
# (The full URL 404s: the provider treats room.id literally.)
OLCRTC_ROOM_ID=


# Leave empty to auto-generate on first run (printed to logs, saved to the
# volume). Paste the generated 64-hex value here to pin it, and into the
# mobile client's OlcrtcClientConfig.keyHex.
OLCRTC_KEY=

# vp8channel throughput tuning (match the mobile client). Bigger batch = faster;
# 64 is the engine max, lower fps = less CPU.
OLCRTC_VP8_FPS=30
OLCRTC_VP8_BATCH=64

# Optional
OLCRTC_DNS=8.8.8.8:53
OLCRTC_DEBUG=false
